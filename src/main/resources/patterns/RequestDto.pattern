package ${project_packet}.dto.request;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
${imports}

@Builder
@Data
@NoArgsConstructor
@AllArgsConstructor
public class ${class_name} {

${fields}
}