package ${project_packet}.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import ${project_packet}.entity.${entity_name};
${imports}

public interface ${class_name} extends JpaRepository<${entity_name}, Long> {
${methods}
}