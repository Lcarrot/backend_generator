package ${packet}.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import ${packet}.entity.${entity_name};
${imports}

public interface ${class_name}Repository extends JpaRepository<${entity_name}, Long> {
${methods}
}