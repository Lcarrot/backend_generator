package ${packet}.service;

import org.springframework.stereotype.Service;
import lombok.RequiredArgsConstructor;
${imports}

@Service
@RequiredArgsConstructor
public class ${class_name}Service {

${fields}
${methods}
}